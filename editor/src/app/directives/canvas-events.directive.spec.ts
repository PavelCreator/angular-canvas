import { CanvasEventsDirective } from './canvas-events.directive';

describe('CanvasEventsDirective', () => {
  it('should create an instance', () => {
    const directive = new CanvasEventsDirective();
    expect(directive).toBeTruthy();
  });
});
